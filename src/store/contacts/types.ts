enum ActionTypes {
  FETCH_CONTACTS = "fetch_contacts",
  FETCH_CONTACT_BY_ID = "fetch_contact_by_id",
}

export default ActionTypes;

// 1 - Créer un type d'actions pour récupérer un seul contact
// 2 - Créer une action(interface)
// 3 - Créer l'ation creator correspondant
// 3 - Mettre à jour le reducer
//
